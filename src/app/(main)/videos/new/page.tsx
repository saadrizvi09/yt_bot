import AddVideoForm from '@/components/AddVideoForm';

export default async function NewVideoPage() {
  // The AddVideoForm component now handles the entire page layout and styling.
  return <AddVideoForm />;
}